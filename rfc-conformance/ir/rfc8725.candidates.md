# Requirement candidates: RFC8725

*February 2020*

32 candidates (MUST: 20, SHOULD: 12), 0 ABNF rules, 25 example blocks, 25 lowercase items to review.

| ID | Level | Section | Conf. | Text |
| --- | --- | --- | --- | --- |
| `RFC8725-S2_8-R01` | MUST/posi | 2.8 | 0.9 | If the JWT could be used in an application context in which it could be confused with other kinds of JWTs, then mitigations MUST be employed to prevent these... |
| `RFC8725-S3_1-R01` | MUST/posi | 3.1 | 0.8 | Libraries MUST enable the caller to specify a supported set of algorithms and MUST NOT use any other algorithms when performing cryptographic operations. |
| `RFC8725-S3_1-R02` | MUST/posi | 3.1 | 0.9 | The library MUST ensure that the "alg" or "enc" header specifies the same algorithm that is used for the cryptographic operation. |
| `RFC8725-S3_1-R03` | MUST/posi | 3.1 | 0.9 | Moreover, each key MUST be used with exactly one algorithm, and this MUST be checked when the cryptographic operation is performed. |
| `RFC8725-S3_2-R01` | SHOULD/posi | 3.2 | 0.9 | Even if a JWS can be successfully validated, unless the algorithm(s) used in the JWS are acceptable to the application, it SHOULD consider the JWS to be inva... |
| `RFC8725-S3_2-R02` | MUST/posi | 3.2 | 0.9 | Therefore, applications MUST only allow the use of cryptographically current algorithms that meet the security requirements of the application. |
| `RFC8725-S3_2-R03` | MUST/posi | 3.2 | 0.9 | Applications MUST therefore be designed to enable cryptographic agility. |
| `RFC8725-S3_2-R04` | SHOULD/proh | 3.2 | 0.9 | JWT libraries SHOULD NOT generate JWTs using "none" unless explicitly requested to do so by the caller. |
| `RFC8725-S3_2-R05` | SHOULD/proh | 3.2 | 0.9 | Similarly, JWT libraries SHOULD NOT consume JWTs using "none" unless explicitly requested by the caller. |
| `RFC8725-S3_2-R06` | SHOULD/posi | 3.2 | 0.9 | Applications SHOULD follow these algorithm-specific recommendations: |
| `RFC8725-S3_2-R07` | SHOULD/posi | 3.2 | 0.9 | To counter these attacks, JWT libraries SHOULD implement ECDSA using the deterministic approach defined in [RFC6979]. |
| `RFC8725-S3_3-R01` | MUST/posi | 3.3 | 0.9 | All cryptographic operations used in the JWT MUST be validated and the entire JWT MUST be rejected if any of them fail to validate. |
| `RFC8725-S3_3-R02` | MUST/posi | 3.3 | 0.9 | This is true not only of JWTs with a single set of Header Parameters but also for Nested JWTs in which both outer and inner operations MUST be validated usin... |
| `RFC8725-S3_4-R01` | MUST/posi | 3.4 | 0.9 | For the NIST prime-order curves P-256, P-384, and P-521, validation MUST be performed according to Section 5.6.2.3.4 (ECC Partial Public-Key Validation Routi... |
| `RFC8725-S3_5-R01` | MUST/posi | 3.5 | 0.9 | The Key Entropy and Random Values advice in Section 10.1 of [RFC7515] and the Password Considerations in Section 8.8 of [RFC7518] MUST be followed. |
| `RFC8725-S3_5-R02` | MUST/proh | 3.5 | 0.9 | In particular, human-memorizable passwords MUST NOT be directly used as the key to a keyed-MAC algorithm such as "HS256". |
| `RFC8725-S3_6-R01` | SHOULD/proh | 3.6 | 0.9 | Compression of data SHOULD NOT be done before encryption, because such compressed data often reveals information about the plaintext. |
| `RFC8725-S3_7-R01` | MUST/posi | 3.7 | 0.9 | Implementations and applications MUST do this and not use or admit the use of other Unicode encodings for these purposes. |
| `RFC8725-S3_8-R01` | MUST/posi | 3.8 | 0.9 | When a JWT contains an "iss" (issuer) claim, the application MUST validate that the cryptographic keys used for the cryptographic operations in the JWT belon... |
| `RFC8725-S3_8-R02` | MUST/posi | 3.8 | 0.9 | If they do not, the application MUST reject the JWT. |
| `RFC8725-S3_8-R03` | MUST/posi | 3.8 | 0.9 | Similarly, when the JWT contains a "sub" (subject) claim, the application MUST validate that the subject value corresponds to a valid subject and/or issuer-s... |
| `RFC8725-S3_8-R04` | MUST/posi | 3.8 | 0.9 | If the issuer, subject, or the pair are invalid, the application MUST reject the JWT. |
| `RFC8725-S3_9-R01` | MUST/posi | 3.9 | 0.9 | If the same issuer can issue JWTs that are intended for use by more than one relying party or application, the JWT MUST contain an "aud" (audience) claim tha... |
| `RFC8725-S3_9-R02` | MUST/posi | 3.9 | 0.9 | In such cases, the relying party or application MUST validate the audience value, and if the audience value is not present or not associated with the recipie... |
| `RFC8725-S3_10-R01` | SHOULD/posi | 3.10 | 0.9 | Applications SHOULD protect against such attacks, e.g., by matching the URL to a whitelist of allowed locations and ensuring no cookies are sent in the GET r... |
| `RFC8725-S3_11-R01` | SHOULD/posi | 3.11 | 0.9 | Per the definition of "typ" in Section 4.1.9 of [RFC7515], it is RECOMMENDED that the "application/" prefix be omitted from the "typ" value. |
| `RFC8725-S3_11-R02` | SHOULD/posi | 3.11 | 0.9 | Therefore, for example, the "typ" value used to explicitly include a type for a SET SHOULD be "secevent+jwt". |
| `RFC8725-S3_11-R03` | SHOULD/posi | 3.11 | 0.9 | When explicit typing is employed for a JWT, it is RECOMMENDED that a media type name of the format "application/example+jwt" be used, where "example" is repl... |
| `RFC8725-S3_11-R04` | MUST/posi | 3.11 | 0.9 | When applying explicit typing to a Nested JWT, the "typ" Header Parameter containing the explicit type value MUST be present in the inner JWT of the Nested J... |
| `RFC8725-S3_11-R05` | SHOULD/posi | 3.11 | 0.9 | Explicit typing is RECOMMENDED for new uses of JWTs. |
| `RFC8725-S3_12-R01` | MUST/posi | 3.12 | 0.9 | If more than one kind of JWT can be issued by the same issuer, the validation rules for those JWTs MUST be written such that they are mutually exclusive, rej... |
| `RFC8725-S3_12-R02` | SHOULD/posi | 3.12 | 0.9 | As discussed in Section 3.11, for new JWT applications, the use of explicit typing is RECOMMENDED. |
