# Requirement candidates: RFC7638

*September 2015*

4 candidates (MUST: 4), 0 ABNF rules, 11 example blocks, 28 lowercase items to review.

| ID | Level | Section | Conf. | Text |
| --- | --- | --- | --- | --- |
| `RFC7638-S3_3-R01` | MUST/posi | 3.3 | 0.9 | Characters in member names and member values MUST be represented without being escaped. |
| `RFC7638-S3_3-R02` | MUST/posi | 3.3 | 0.9 | If the JWK key type uses members whose values are themselves JSON objects, then the members of those objects MUST likewise be lexicographically ordered. |
| `RFC7638-S3_3-R03` | MUST/posi | 3.3 | 0.9 | If the JWK key type uses members whose values are JSON numbers, and if those numbers are integers, then they MUST be represented as a JSON number as defined ... |
| `RFC7638-S3_3-R04` | MUST/posi | 3.3 | 0.9 | MUST be represented as "1024". |
