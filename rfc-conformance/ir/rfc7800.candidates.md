# Requirement candidates: RFC7800

*H. Tschofenig*

10 candidates (MAY: 1, MUST: 8, SHOULD: 1), 0 ABNF rules, 24 example blocks, 18 lowercase items to review.

| ID | Level | Section | Conf. | Text |
| --- | --- | --- | --- | --- |
| `RFC7800-S3-R01` | MUST/posi | 3 | 0.9 | At least one of the "sub" and "iss" claims MUST be present in the JWT. |
| `RFC7800-S3_1-R01` | MUST/posi | 3.1 | 0.9 | However, in the absence of such requirements, all confirmation members that are not understood by implementations MUST be ignored. |
| `RFC7800-S3_1-R02` | MUST/posi | 3.1 | 0.9 | The "cnf" claim value MUST represent only a single proof-of- possession key; thus, at most one of the "jwk", "jwe", and "jku" (JWK Set URL) confirmation valu... |
| `RFC7800-S3_2-R01` | MUST/posi | 3.2 | 0.8 | The JWK MUST contain the required key members for a JWK of that key type and MAY contain other JWK members, including the "kid" (Key ID) member. |
| `RFC7800-S3_2-R02` | MAY/posi | 3.2 | 0.9 | The "jwk" member MAY also be used for a JWK representing a symmetric key, provided that the JWT is encrypted so that the key is not revealed to unintended pa... |
| `RFC7800-S3_2-R03` | MUST/posi | 3.2 | 0.9 | If the JWT is not encrypted, the symmetric key MUST be encrypted as described below. |
| `RFC7800-S3_5-R01` | MUST/posi | 3.5 | 0.9 | If there are multiple keys in the referenced JWK Set document, a "kid" member MUST also be included with the referenced key's JWK also containing the same "k... |
| `RFC7800-S3_5-R02` | MUST/posi | 3.5 | 0.9 | The protocol used to acquire the resource MUST provide integrity protection. |
| `RFC7800-S3_5-R03` | MUST/posi | 3.5 | 0.9 | An HTTP GET request to retrieve the JWK Set MUST use TLS [RFC5246] and the identity of the server MUST be validated, as per Section 6 of RFC 6125 [RFC6125]. |
| `RFC7800-S6_2_1-R01` | SHOULD/posi | 6.2.1 | 0.9 | Because a core goal of this specification is for the resulting representations to be compact, it is RECOMMENDED that the name be short -- not to exceed eight... |
