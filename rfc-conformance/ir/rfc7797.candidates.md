# Requirement candidates: RFC7797

*JSON Web Signature (JWS) Unencoded Payload Option*

13 candidates (MAY: 2, MUST: 9, SHOULD: 2), 0 ABNF rules, 16 example blocks, 5 lowercase items to review.

| ID | Level | Section | Conf. | Text |
| --- | --- | --- | --- | --- |
| `RFC7797-SAbstract-R01` | MUST/proh | Abstract | 0.9 | This specification updates RFC 7519 by stating that JSON Web Tokens (JWTs) MUST NOT use the unencoded payload option defined by this specification. |
| `RFC7797-S3-R01` | MUST/posi | 3 | 0.9 | When used, this Header Parameter MUST be integrity protected; therefore, it MUST occur only within the JWS Protected Header. |
| `RFC7797-S3-R02` | MAY/posi | 3 | 0.65 | Use of this Header Parameter is OPTIONAL. |
| `RFC7797-S3-R03` | MUST/posi | 3 | 0.9 | If the JWS has multiple signatures and/or MACs, the "b64" Header Parameter value MUST be the same for all of them. |
| `RFC7797-S5_1-R01` | MUST/posi | 5.1 | 0.9 | If an application uses a content encoding when representing the payload, then it MUST specify whether the signature or MAC is performed over the content-enco... |
| `RFC7797-S5_2-R01` | MUST/proh | 5.2 | 0.9 | When using the JWS Compact Serialization, unencoded non-detached payloads using period ('.') characters would cause parsing errors; such payloads MUST NOT be... |
| `RFC7797-S5_2-R02` | MUST/posi | 5.2 | 0.9 | Similarly, if a JWS using the JWS Compact Serialization and a non-detached payload is to be transmitted in a context that requires URL-safe characters, then ... |
| `RFC7797-S5_2-R03` | MAY/posi | 5.2 | 0.9 | (those characters in the ranges %x20-2D and %x2F-7E) MAY be included in a non-detached payload using the JWS Compact Serialization, provided that the applica... |
| `RFC7797-S5_3-R01` | MUST/proh | 5.3 | 0.9 | Unassigned Unicode code point values MUST NOT be used to represent the payload. |
| `RFC7797-S6-R01` | MUST/posi | 6 | 0.9 | The "crit" Header Parameter MUST be included with "b64" in its set of values when using the "b64" Header Parameter to cause implementations not implementing ... |
| `RFC7797-S7-R01` | SHOULD/proh | 7 | 0.9 | It is NOT RECOMMENDED that this parameter value be dynamically varied with different payloads in the same application context. |
| `RFC7797-S7-R02` | SHOULD/posi | 7 | 0.9 | While it is legal to use "b64" with a "true" value, it is RECOMMENDED that "b64" simply be omitted in this case, since it would be selecting the behavior alr... |
| `RFC7797-S7-R03` | MUST/proh | 7 | 0.9 | For interoperability reasons, JSON Web Tokens [JWT] MUST NOT use "b64" with a "false" value. |
